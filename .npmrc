# Expo SDK 54 pins react@19.1.0 (must match react-native-renderer). Some deps
# (e.g. @clerk/clerk-react) list a slightly narrower peer range; legacy peer
# resolution keeps a single correct React version without npm ERESOLVE failures.
legacy-peer-deps=true
